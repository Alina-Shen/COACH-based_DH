# Archived Q-Chem-orbital route

This directory is a non-destructive snapshot of the files that directly
implemented, specified, validated, or documented the retired Q-Chem-orbital
route for `revwb97m2`.

- Snapshot date: 2026-08-29 (America/Los_Angeles)
- Source repository commit at snapshot: `58c4063737f76e5d80b02b2aa9cb7a375535c05f`
- Original relative paths are preserved below `revwb97m2/`.
- The original files were copied, not moved or deleted.
- `MANIFEST.sha256` verifies every archived code/configuration file.

The route's accepted smoke artifacts and prepared gateway snapshot are stored
at `/clusterfs/mhg-data/yaoshen/coach-based_dh_data/qchem_orbital_route`.
The 14,006-species authoritative Q-Chem input publication was not duplicated:
it is immutable provenance/input metadata rather than a smoke test and remains
at `/clusterfs/mhg-data/yaoshen/coach-based_dh_data/revwb97m2/authoritative_inputs/qchem/gscdb137_v1`.

Specification version 3 forbids these archived Q-Chem orbitals as production
inputs. They remain available for provenance and cross-engine regression.
