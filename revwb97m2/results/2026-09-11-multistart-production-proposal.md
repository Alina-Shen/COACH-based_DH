# Larger pool plus COACH noisy repeats — approved direction, seed choice pending

User reverses prior mismatch1 decision:adopt COACH noise around ORIGINAL start,not preceding solution. Larger pool approved. Retain coarseKlist and paper/GSCDBNER policies;record lean28and incumbentrestart as futureTODOs,not current execution.

Review-only config: `revwb97m2/configs/production_multistart_v1.proposal.yaml`. No existing runner reads this proposal and bulk_authorized=false. Counts:7K x1simple x2repeats=14pass1;7K x(simple+2matchingpass1vectors) x2repeats=42pass2;total56.7200s each gives112solver-hours/1792CPU-hours at16CPU,before overhead;parallel elapsed depends on scheduling. All14discovery results contribute to row selection;no hardcoded349rows or automatic candidate deduplication. Pass2waits for full declared discovery pool and frozen selected rows. SameKpass2starts are separate searches,not pooled starts in a single solve.

Source checked again:`coach/2_optimization/coachopt/optimizer.py:29,71-93,199-223`. No current solver/spec files changed. For everystart repeat0 uses originalvector;repeat1 uses originalvector+N(0,0.05²) on EVERY coefficient,including zeros. This can produce dense,infeasible initial suggestions,including cardinality/UEG/scalar-bound violations. Do not silently clip,project or reject merely for infeasibility;new production start-import path must distinguish suggestions from accepted solutions. Keep strict fitted-solution audits. Existing recovery start_audit cannot be reused unmodified for this noise path. Tests must cover that distinction and ensure noisy starts never loosen model constraints. This is not evidence fitting itself will be infeasible.

Propose precomputing/hash-storing vectors in fixed K/start order using NumPydefault_rng(0),reset once per pass as with separate COACHsweeps;parallel job order must not alter draws.292features necessarily differ from original289drawsequence;do not claim bit-identical noise across models. Freeze RNG/order/selection suggestions with production plan. Current binary-link semantics remain approvedbigM,mandatoryscalarselection,not COACH'sSOS1iszero model.

Missing simple-seed choice:COACHprovides289vector with indices0=.85,1=1,96=1,192=1,288=.15. Our addedVV10/PT2/ATMscalar starts are not determined by that vector. Recommend copying ONLY indices289:292 from validated25792248/constrained80:VV10=.99999999,PT2=.027300022669629182,ATM=.99999999,while keeping COACHsemilocal/HFsimple seed. This is a proposed project-specific initialization,NOT originalCOACHconstants or fixedproductioncoefficients. These two bound-adjacent values mean noisy repeats may start outside bounds;handled as documented suggestions. User approval required before selecting these values. Alternative user-specified triple accepted for review.

After seed decision:implement versioned production adapter/noisy-start handling/immutable task graph,regressions and short import/build preflight;freeze updated effective specification without rewriting historical hashes;stop with commit commands;then tests/resources/pre-bulkrelease. No new chemical generation or already-successful generic solver-pilot repetition needed. No jobs submitted,tests not rerun in this planning turn.
